package Google;



//input: given a list of circles, upper bound of a tunnel,
// 返回一个boolean问这些circles有没有block这个tunnel, circles有坐标(x,y)和半径radius。
// tunnel的lower bound是0, upper bound是Y
public class BlockTunnel {


    // idea: union find
    public boolean block(int[][] circles, int y){
        return false;
    }

    public boolean checkIfblock(){
        return false;
    }
}
