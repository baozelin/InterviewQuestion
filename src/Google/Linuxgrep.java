package Google;


import java.util.List;

// linux grep方法 grep xx --context=1 => 输出所有等于xx的内容
//eg. content = [ab, cd, cd, ef, xyz], context = 1, word = cd. output: [ab, cd, cd, ef].  
// 如果word = ab其他不变，output: [ab, cd]
public class Linuxgrep {


    public List<String> grep(String word, int context, List<String> content){
        return null;
    }
}
